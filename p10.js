/* Write a function to convert a string to lowercase. The function should take a single argument, which is the string to convert.
function toLowerCase(str) {
  return ...;
}

console.log(toLowerCase("HELLO WORLD")); // "hello world"
console.log(toLowerCase("JavaScript")); // "javascript" */

function toLowerCase(str) {
    return str.toLowerCase();
  }
  
  console.log(toLowerCase("HELLO WORLD")); 
  console.log(toLowerCase("JavaScript")); 

  /* str.toLowerCase():
This method converts all characters in the string str to lowercase and returns the new string.
The toLowerCase method is built into JavaScript's String object, making it a straightforward way to perform this conversion. */
  